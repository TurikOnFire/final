# FINAL — Spring Boot Backend

## 📌 Описание проекта

**FINAL** — это backend‑приложение на **Spring Boot**, реализующее REST API с аутентификацией на основе **JWT**. Проект использует **PostgreSQL** в качестве основной БД, **Liquibase** для управления миграциями, а также интеграции с **Redis** и **Kafka**. Приложение может быть запущено локально или в Docker‑окружении.

Проект предназначен как основа для production‑ready сервиса с расширяемой архитектурой.

---

## 🧱 Стек технологий

- Java 21
- Spring Boot
- Spring Security + JWT
- Spring Data JPA (Hibernate)
- PostgreSQL
- Liquibase
- Redis
- Apache Kafka
- Maven
- Docker / Docker Compose
- Thymeleaf (если используется UI)

---

## 📂 Структура проекта (укрупнённо)

```
final/
├── src/
│   ├── main/
│   │   ├── java/...        # Java‑код приложения
│   │   └── resources/
│   │       ├── application.yaml
│   │       ├── db/changelog # Liquibase миграции
│   │       └── templates/   # Thymeleaf шаблоны (если есть)
│   └── test/               # Unit и integration‑тесты
├── docker/                 # Docker конфигурации
├── docker-compose.yml
├── pom.xml
└── README.md
```

---

## ⚙️ Конфигурация

Основная конфигурация находится в `application.yaml`.

Пример переменных окружения:

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_SCHEMA=final
DB_USER=postgres
DB_PASSWORD=postgres

REDIS_HOST=localhost
REDIS_PORT=6379

KAFKA_BOOTSTRAP_SERVERS=localhost:9092
JWT_SECRET=your_secret_key
```

---

## 🔐 Аутентификация

В проекте используется **JWT‑аутентификация**:

- Токен генерируется при логине пользователя
- JWT содержит username
- Срок жизни токена — 1 час
- Все защищённые эндпоинты требуют `Authorization: Bearer <token>`

Пример сервиса:
- `JwtService` — генерация и валидация токенов

---

## 🗄️ Работа с БД

- Используется **PostgreSQL**
- ORM: **Hibernate (JPA)**
- Миграции БД управляются через **Liquibase**

Liquibase:
- changelog‑файлы находятся в `resources/db/changelog`
- Миграции применяются автоматически при старте приложения

---

## 📬 Redis и Kafka

### Redis
Используется для:
- кеширования
- хранения временных данных (опционально)

### Kafka
Используется для:
- асинхронного обмена сообщениями
- событийной архитектуры

(Конкретные топики и продюсеры/консьюмеры зависят от реализации)

---

## 🚀 Запуск проекта

### Локально

```bash
mvn clean install
mvn spring-boot:run
```

Приложение будет доступно по адресу:

```
http://localhost:8080
```

---

### 🐳 Docker Compose

```bash
docker-compose up --build
```

Будут запущены:
- backend‑приложение
- PostgreSQL
- Redis
- Kafka

---

## 🧪 Тестирование

В проекте используются:
- JUnit 5
- Mockito

Запуск тестов:

```bash
mvn test
```

---

## 📌 TODO / Возможные улучшения

- Refresh‑tokens
- Role‑based access control (RBAC)
- Swagger / OpenAPI
- Monitoring (Prometheus + Grafana)
- Integration tests с Testcontainers

---

## 👤 Автор

Проект разработан в рамках учебного / pet‑проекта и может свободно расширяться и модифицироваться.

---

Если нужно — могу:
- адаптировать README под **GitHub**
- упростить его для **pet‑проекта**
- дописать разделы **API / Swagger / Security / Kafka flows**

